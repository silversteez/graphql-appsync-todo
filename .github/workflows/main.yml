name: CI

on:
  push:
    branches: [ main ]
#   pull_request:
#     branches: [ main ]


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: install deps
        run: yarn

      - name: deploy
        run: yarn deploy:prod
